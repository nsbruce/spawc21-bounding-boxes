# spawc21-bounding-boxes

The bounding box half of the spawc challenge

## Requirements

- rfinder
- sigmf (from master, not pypi)
- python-dotenv

## Getting started

Copy env.sample into a .env file and fill in the variables as needed.
